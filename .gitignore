# Secrets and local configuration
.env
config/repositories.yaml

# Python
__pycache__/
*.py[cod]
.venv/
build/
dist/
*.egg-info/

# Tooling
.pytest_cache/
.ruff_cache/
.coverage
htmlcov/

# Local run logs
setup.log

# Docker credential file (ADR-0007). Never committed.
.env.docker

# The shared server's credential vault (ADR-0008). Encrypted, and still never
# committed: a repository is exactly where a stolen file comes from.
config/credentials.sqlite3
config/credentials.sqlite3-*
*.vault.key
